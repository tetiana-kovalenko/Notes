<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/itemSaveNote"
        app:showAsAction="always"
        android:icon="@drawable/ic_save_note"
        android:title="Save Note"
        android:visible="false"/>
    <item android:id="@+id/itemAddOrChangeThumb"
        app:showAsAction="always"
        android:icon="@drawable/ic_add_thumb"
        android:title="Add Thumbnail"/>
    <item android:id="@+id/otherItems"
        android:title="Other"
        android:visible="true"/>
</menu>